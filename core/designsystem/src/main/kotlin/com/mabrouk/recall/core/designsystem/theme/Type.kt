package com.mabrouk.recall.core.designsystem.theme

import androidx.compose.material3.Typography

// Recall uses the platform default type scale (system font, e.g. Roboto).
// This respects the user's font-size / display settings and keeps the app
// aligned with Android system design. The full Material 3 type scale is
// provided by Typography() defaults.
val RecallTypography = Typography()
